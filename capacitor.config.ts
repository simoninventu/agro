import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.inventu.cotizadoragro',
  appName: 'Cotizador Agro',
  webDir: 'dist'
};

export default config;
